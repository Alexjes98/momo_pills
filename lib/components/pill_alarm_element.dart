import 'package:flutter/material.dart';

class PillAlarmElement extends StatelessWidget {
  const PillAlarmElement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: const Text('Pill Alarm'),
      onTap: () {
        
      },
    );

  }
}